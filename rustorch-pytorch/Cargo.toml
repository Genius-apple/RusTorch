[package]
name = "rustorch-pytorch"
version = "0.1.2"
edition = "2021"

[dependencies]
rustorch-core = { path = "../rustorch-core", version = "0.1.2" }
tch = { version = "0.13", optional = true }
anyhow = "1.0"

[features]
default = []
pytorch_backend = ["dep:tch"]
