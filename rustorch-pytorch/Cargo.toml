[package]
name = "rustorch-pytorch"
version = "0.1.1"
edition = "2021"

[dependencies]
rustorch-core = { path = "../rustorch-core", version = "0.1.1" }
tch = "0.13" # Or latest compatible version
anyhow = "1.0"
